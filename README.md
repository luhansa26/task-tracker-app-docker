# task-tracker-app-docker
A simple Node.js/Express REST API for tracking tasks (in-memory storage), containerized with Docker and pushed to Docker Hub/ECR — built as a hands-on exercise in Dockerizing and shipping a sample app.
